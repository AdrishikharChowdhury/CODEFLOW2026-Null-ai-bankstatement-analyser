FROM python:3.11-slim AS base

COPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/uv

WORKDIR /app

COPY model/pyproject.toml model/uv.lock ./
RUN uv sync --frozen --no-dev

COPY model/ .

EXPOSE 8001

CMD ["uv", "run", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
